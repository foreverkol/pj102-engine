# 跨平台行尾一致性（2026-09-16，配合实例侧 D-34）
#
# 为什么必须显式声明：Windows 默认 core.autocrlf=true，checkout 时会把仓库里的
# LF 转为 CRLF —— 分发出的代码在 POSIX 环境跑就会写回 CRLF 文件，
# 直接抵消 D-34「wiki 全库统一 LF」的成果。
#
# 本仓为**引擎发行物**：一切文本一律 LF。
* text=auto eol=lf

# 二进制资源不做转换
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.zip  binary
*.whl  binary

# Windows 专用脚本允许 CRLF（cmd 对 LF 容忍度差）
*.bat  text eol=crlf
*.cmd  text eol=crlf
